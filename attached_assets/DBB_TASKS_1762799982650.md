# Implementation Tasks (Replit Agent)

0) Setup tokens: add `src/styles/tokens.css` and import globally.

1) Build homepage UI (hero, discover, categories, community, supplier).
2) Build business page UI (gallery, CTAs, services, hours, reviews, Q&A).
3) Create core components: BusinessCard, CategoryChip, RatingStars, SaveButton, PromoBadge.
4) Add `SeoHead.tsx` + JSON-LD helpers.
5) Add sitemaps + robots routes in Express.
6) Wire search to Postgres FTS + trigram (ranking by featured/verified/proximity/rank).
7) Add `/api/report` + rate limiters.
